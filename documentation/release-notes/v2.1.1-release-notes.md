# Legendary: Marvel Randomizer — v2.1.1 Release Notes

**Released: May 2026**

> Fan-made tool. Not affiliated with Marvel or Upper Deck Entertainment.

---

## What is this?

Legendary: Marvel Randomizer is a free, browser-based companion app for players of *Legendary: A Marvel Deck-Building Game*. Open the URL. Get a legal, fresh, collection-aware game setup in seconds. No install. No account. No tracking. Ever.

**[Try it now →](https://alban34.github.io/random-legendary-llm)**

---

## What's in v2.1.1

v2.1.1 is a code-quality release. It resolves all SonarCloud-reported issues (99 issues across Critical, Major, and Minor severities) covering cognitive complexity reductions, type assertion cleanup, test modernization, and style improvements (Epic 93). It also reduces test duplication across 15 test files by extracting shared helpers (Epic 94) and significantly expands unit test coverage from 63.3% to 74.2% statement coverage by adding 120 new tests in 9 new test files (Epic 95). No user-facing behavior changes are included.

---

### Critical: cognitive complexity reductions

Cognitive complexity was reduced in `result-utils.ts`, `stats-utils.ts`, and `setup-generator.ts` by extracting nested conditional logic into helper functions and flattening branching chains. Sort calls in test files were updated to use a `localeCompare`-based comparator where required by SonarCloud.

---

### Major: modernization and correctness fixes

- `bgg-import-utils.ts`: redundant `undefined` removed from function signatures
- `collection-utils.ts`: `.sort()` replaced with `.toSorted()` for non-mutating sort behavior
- `browser-entry.ts`: top-level `await` replaced with an explicit async wrapper
- `setup-generator.ts`: related parameters grouped into typed objects to reduce parameter count
- `setup-generator.test.ts`: nested template literals replaced with string concatenation

---

### Minor: language-modernization sweep

A broad pass across many files replaced legacy patterns with modern equivalents: type assertions removed where types were already inferred; `||` replaced with `??` for nullish checks; `Object.hasOwn()`, `structuredClone()`, `Set` membership checks, `String#replaceAll()`, `String.raw`, `globalThis`, `TypeError`, and `export … from` re-export syntax introduced where applicable; union type redundancies removed.

---

### Code duplication reduction (Epic 94)

Thirteen duplicate test cases were removed by extracting shared test helpers into a new `src/app/test-utils.ts` module. The helpers `createMemoryStorage`, `minimalIndexes`, `createAllOwnedState`, and `createSampleSnapshot` were pulled from 15 test files that previously defined them inline. The `mergeOwnedSets` test suite was consolidated from `myludo-import-utils.test.ts` and `bgg-import-utils.test.ts` into the canonical `collection-utils.test.ts`. Verbatim duplicate test blocks were also removed from `setup-rules.test.ts`, `BrowseTab.test.ts`, and `theme-utils.test.ts`. The unit test count decreased from 480 to 467 as a direct result of removing duplicate coverage. No production behaviour was changed.

---

### Test coverage improvement (Epic 95)

Unit test coverage was significantly expanded across the most under-tested modules.

**Before:** 63.3% stmt / 60.0% branch — 467 tests in 33 files  
**After:** 74.2% stmt / 75.1% branch — 587 tests in 42 test files

Nine new test files were added:
- `src/app/app-init.test.ts`, `src/app/backup-vm.test.ts`, `src/app/browse-vm.test.ts`, `src/app/history-vm.test.ts`, `src/app/import-vm.test.ts`, `src/app/state-store.svelte.test.ts`
- `src/components/ModalRoot.test.ts`, `src/components/TabNav.test.ts`, `src/components/ToastStack.test.ts`

Notable coverage improvements:

| Module | Before | After |
|--------|--------|-------|
| `localization-utils.ts` | 32.1% stmt | 97.3% stmt |
| `forced-picks-utils.ts` | 34.4% stmt | 100% stmt |
| `preferences-actions.ts` | 0% stmt | 100% stmt |

All other targetable utility modules now reach ≥ 80% statement and branch coverage. The one documented exception is `app-tabs.ts`, which remains at 73.3% branch because the four uncovered branches are structurally unreachable null-coalescing paths when `TAB_IDS` is empty — a condition that is impossible at runtime.
