# List of needs for v1.0.4

- [x] The PWA, once installed through Safari on iPad, does not seem allow to import data from the json file: is there something we can do for this or is it a security that we won't be able to bypass? If so, please find another way to import data
- [x] All user facing sentences have to be revisited: they don't care about my "Epic", they should never be mentionned in the UI
- [x] Update solo behavior based on `solo-mode-rules.md` file and add a new mode "Standard v2"
- [x] When playing solo, add a rule section (where?) to explain the steps corresponding to the different change of rules that applies to solo (stated in the solo-mode-rules.md file)
- [x] In multiplayer, score part must be one per player (and allows to set the name of the player when entering scores). The win or lose is still a single entry though
- [x] Add a "draw" entry: this happens when the players do not beat the mastermind but survive to his scheme
- [x] Allow to sort sets browser by release date, name, in collection or not
